div.settings
    form(method="get", action="/")
        input.stationSearch
        ////if station
//- /            - currentlyListening = station.code;
        //- select(name="stationCode", id="stationCode", data-currently-listening=currentlyListening, placeholder='Please select...')
        //-     each station in stationCodes
        //-         var str = { code:  station.id }
        //-         option(value=station.slug, data-code=str)=station.name
div.clear